# Scenario: what must be accomplished for the current task.
#
# The scenario format is intentionally minimal and experimental; see
# docs/scenario_format.md.

name: sample

description: >
  Verify basic connectivity between R1 and R2 and summarize the evidence.

objectives:
  - obtain the active topology
  - open a terminal session on R1 and R2
  - inspect their current state and confirm the link between them is up
  - summarize the findings with the evidence gathered
